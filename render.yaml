services:
  - type: web
    name: telltale
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      # Set these in the Render dashboard; do not commit credentials.
      - key: NEBIUS_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
